# bugs

Essential imports
for python (>=3.6).
See install_requires in [setup.py](setup.py).

Installation:

```commandline
pip install --upgrade bugs
```

Proposed usage:

```python
from bugs import *
```
